export function buildPrompt(topic: string, answers: Record<string, any>) {
  return `You are generating a minimal learning roadmap strictly as JSON.\n\nUser topic: ${topic}\nUser answers: ${JSON.stringify(answers)}\n\nReturn ONLY JSON matching this schema:\n{\n  "topic": string,\n  "level": "beginner"|"intermediate"|"advanced",\n  "modules": [\n    {\n      "id": string,\n      "title": string,\n      "summary": string,\n      "duration_hours": number,\n      "resources": [\n        { "type": "video"|"article"|"exercise"|"project"|"github", "title": string, "url": string, "estimated_minutes"?: number }\n      ]\n    }\n  ]\n}\nConstraints:\n- 4–8 modules.\n- Each module must include at least one resource with a valid URL.\n- Prefer reputable sources (YouTube lecturers, MOOCs, docs, GitHub).\n- Tailor difficulty using the user answers.\n- Output ONLY valid JSON, no backticks, no prose.`
}
